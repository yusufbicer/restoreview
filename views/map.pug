extends layout

block content
  .inner
    h2= title 
    .map
      .autocomplete
        input.autocopmlete__input(type="text" placeholder="Search for Anything" name="geolocate")
      #map
        p Loading Map...
